export default function MapPlaceholder() {
  return (
    <div
      style={{
        height: 420,
        border: "1px solid #e5e5e5",
        borderRadius: 16,
        background: "#fafafa",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        color: "#666",
      }}
    >
      Interactive Map Placeholder (D3 will go here)
    </div>
  );
}
